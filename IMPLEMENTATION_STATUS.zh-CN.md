# KiteShell 实现与发布状态

文档版本：V1.4
更新时间：2026-07-27

当前交付版本为 `0.5.3-alpha`（Build 21）。PRD 中可以仅依靠本机能力落地的核心连接、终端、真实监控、远程文件、编辑回传、命令库、诊断、备份、跳板机、无认证上游代理、端口转发、传输中心与工作区恢复链路已经接通。

## 已验证

- Swift 单元与模型测试：28 项通过，其中 FinalShell 密码测试与 Java 17 参考结果一致，本地凭据测试验证 AES-GCM 密文不包含明文密码且文件权限为 `600`。
- 自测脚本：6/6 通过。
- 隔离密码 SSH：真实系统 OpenSSH + AskPass 通过。
- 隔离 SFTP：ControlMaster 下载、修改、覆盖上传和内容校验通过。
- Release App：固定 `KiteShell Local Code Signing` 身份签名并通过严格校验。
- 主机密钥：首次连接使用 OpenSSH `accept-new` 静默记录；已保存 ED25519 密钥仍从含空格的 Application Support 路径读取，密钥变化时拒绝连接。
- 连接性能：完全移除连接前 `ssh-keyscan` 与指纹确认前置流程；此前该服务器前置扫描实测 2.36 秒。
- 凭据：密码和私钥口令使用当前用户本地 AES-GCM 凭据库，不再调用 Keychain；旧 Keychain 项不会自动读取，已有连接需重新输入并保存一次。
- FinalShell：支持 JSON/嵌套结构连接与 `Random + MD5 + DES/ECB/PKCS5Padding` 密码解析；成功密码转存本地凭据库，失败项只统计数量，不记录敏感值。

## 不能在本地伪造为完成的发布条件

- Developer ID Application 正式签名、Apple 公证与 Gatekeeper 公网分发验证，需要 Apple Developer 团队证书。
- 应用内自动更新需要确定发布渠道、更新清单 URL、更新签名密钥和回滚服务。
- iCloud 同步、AI 服务、团队空间与审计需要单独的账号、服务端、隐私和产品决策。
- 8/24 小时长稳、2 GB 传输及所有 Linux 发行版/公网/VPN/IPv6 测试矩阵需要对应时间与真实测试环境；本次没有伪造测试结果。

## 当前明确边界

- 本地凭据库的加密密钥与密文都属于当前 macOS 用户，主要防止明文泄露和其他用户读取；它不提供 Keychain/安全隔区级别的同账户攻击防护。
- 上游 SOCKS5/HTTP CONNECT 首版支持无认证代理；带凭据代理需要独立安全隧道实现，不能将密码拼入命令行。
- 上传支持真正暂停、继续、取消和失败重试；协议中断后的断点续传仍会从头开始。
- 普通 SSH 标签可安全恢复为待重连入口，但不会自动登录或重放命令。
- 当前窗口支持多会话标签和拖动排序；标签拖出为独立窗口及同一窗口终端分屏仍属于后续原生窗口架构工作。
