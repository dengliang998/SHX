# Changelog

## 1.1.1 — 2026-08-11

- 增加 macOS/BSD 远端监控数据采集；部分指标无法获取时单独提示，不再导致检查器整体报错或布局异常。
- 远程目录读取移除 GNU `find -printf` 依赖，改为同时兼容 Linux 与 macOS，并安全处理空格和 UTF-8 文件名。
- 远程文件大小和版本检测增加 macOS `stat` 兼容路径。
- 终端仅在自身获得焦点时处理 `⌘C/⌘V`，路径框、搜索框和内联编辑框恢复标准复制粘贴。
- 远程文件重命名改为列表内直接编辑、保存或取消，不再弹出重命名窗口。
- 修正端口转发空状态窗口的尺寸和布局。
- 项目采用 Apache License 2.0。
- 版本升级为 1.1.1（Build 111）。

## 1.1.0 — 2026-07-31

- 新建和编辑连接的分组改为下拉选择；分组只允许从连接中心左侧栏新增、重命名和删除。
- 增加连接多选，可批量设置分组、添加/移除标签、收藏、取消收藏和删除；每次批量变更只持久化一次。
- 内置“内网”“外网”标签，同时支持自定义标签、标签复用、连接卡片展示和标签搜索。
- 设置页增加中文/English 切换，应用包内同时包含简体中文和英文资源。
- 更新检查支持注入发布源并补充自动化发布夹具，覆盖已是最新、新版本、签名校验和私有/不可访问发布源等路径。
- GitHub 返回 403/404 时给出明确处理建议，不再只显示无意义的响应错误。
- 版本升级为 1.1.0（Build 110）。

## 0.5.3-alpha

- FinalShell 导入新增与 Java 实现一致的密码解析，兼容 `Random + MD5 + DES/ECB/PKCS5Padding` 格式。
- 解析成功的密码直接写入当前用户的本地 AES-GCM 凭据库，不进入连接配置、导出文件或日志。
- 重复导入已有连接时不创建副本，但会用导入结果更新该连接的本地密码。
- 导入结果分别展示连接、重复项、密码保存成功、密码解析失败和本地保存失败数量。

## 0.5.2-alpha

- 密码和私钥口令改存当前用户的本地 AES-GCM 加密凭据库，不再调用 macOS Keychain，更新应用后不会触发钥匙串授权。
- 本地凭据目录权限固定为 `700`，密钥与凭据文件权限固定为 `600`；连接配置、导出文件和日志仍不包含凭据。
- 首次主机密钥由系统 OpenSSH 使用 `accept-new` 在后台自动记录，不再展示主机指纹确认窗口；已记录密钥发生变化时仍拒绝连接。
- 新建连接默认使用密码认证，并压缩标题、表单行、分区及底部操作区的纵向间距。

## 0.5.1-alpha

- 优化已信任服务器连接路径：不再在打开会话前重复执行阻塞式 `ssh-keyscan`，实测消除约 2.36 秒前置等待；真实 OpenSSH 仍使用严格指纹校验。
- 新建和编辑连接页可直接输入 SSH 密码，可选择保存至 macOS Keychain，并提供“仅保存”和“保存并连接”。
- 新增可选连接后初始化命令，默认关闭，并显示自动执行风险提示。
- 连接中心增加按连接状态排序。
- 调整连接中心、文件工具栏和命令库同一行按钮配色，区分返回、导入、排序、快速连接、新建和危险操作。

## 0.5.0-alpha

- 修复严格主机校验无法读取含空格的 Application Support 信任文件、导致已确认 ED25519 指纹仍被拒绝的问题。
- 新增快速连接、可持久化空分组、分组新建/重命名/删除和连接拖放移动。
- 新增 SSH Agent 身份检查、无认证 SOCKS5/HTTP CONNECT 上游代理和更完整的终端个性化设置。
- 新增跨会话传输中心、上传暂停/继续、后台任务通知和远程编辑版本冲突保护。
- 新增内置 UTF-8 文本编辑器，支持 LF/CRLF、查找计数、保存上传和远端版本冲突阻断。
- 命令库扩展为当前服务器与全局范围，并增加不记录正文/输出的最近执行元数据。
- KiteShell 配置备份升级为 Schema 2，可导出空分组和全局命令，仍不包含凭据。
- 增加缓存清理、卸载说明、签名类型、开源许可和内置首次使用帮助。
- 扩展连接配置：标签、备注、连接超时、心跳、启动目录、自动重连、指定私钥和单级跳板机。
- 新增地址与端口测试、分阶段连接诊断、脱敏诊断报告和本地运行日志。
- 新增原生主机指纹确认、指纹变化阻断和已信任服务器管理。
- 新增 KiteShell 无凭据导入导出、OpenSSH Config 导入、连接复制和多种排序。
- 启动时安全恢复上次会话标签与布局，不自动登录或执行历史命令。
- 远程文件增加路径跟随锁定、前进后退、筛选、排序、隐藏文件、文件夹下载和移动操作。
- 上传任务增加真实取消、失败重试、速度与预计剩余时间；取消会终止底层 SFTP 进程。
- 服务器监控增加网络实时速率、进程搜索/排序和确认后发送 SIGTERM。
- 命令脚本增加搜索、标签、运行时变量、插入终端/确认运行/直接运行模式及高风险二次确认。
- 新增本地、远程、动态 SOCKS5 端口转发，可通过当前 ControlMaster 会话立即启动或停止。
- 新增终端内容搜索与会话标签拖动排序。

## 0.4.9-alpha

- 使用登录钥匙串中的固定 `KiteShell Local Code Signing` 身份签名，消除 ad-hoc CDHash 随构建变化导致的逐项 Keychain 授权。
- 密码存储迁移到由固定签名创建的 `com.kiteshell.credentials.v3` 服务；旧项只静默读取，不再触发系统授权弹窗。
- 构建环境缺少固定证书时保留 ad-hoc 回退并输出明确警告，方便源码在其他 Mac 上构建。

## 0.4.8-alpha

- 本地网络授权成功后持久化确认状态，后续内网 SSH 直接启动，不再重复执行授权前置探测。
- 首次授权探测的异常等待从 15 秒缩短到 2 秒，避免网络策略状态异常时连接长时间停留在“连接中”。

## 0.4.7-alpha

- 内网 SSH 启动前由 KiteShell 主进程使用 `Network.framework` 探测私有地址，可靠触发 macOS 本地网络授权。
- 钥匙串读取和删除改为禁止系统认证弹窗；旧密码仅静默迁移，无法静默访问时改用应用内密码框。
- 密码存储迁移到稳定的 `com.kiteshell.credentials.v2` 服务，重新记住一次后不再反复弹出“获取密钥信息”。

## 0.4.6-alpha

- 增加 macOS 本地网络隐私用途说明，使局域网 SSH、文件传输和系统监控能够触发并持久化系统授权。
- 针对 macOS 27 的局域网隐私策略补齐应用元数据，避免内网地址在认证前直接返回 `No route to host`。

## 0.4.5-alpha

- 应用启动时显式加载包内 `.icns` 作为运行时 Dock 图标，避免 LaunchServices 缓存导致控制台图标缺失。
- 将当前 macOS 不支持的 `exclamationmark.terminal` 替换为可用的终端图标。
- 复核 FinalShell 导入地址、端口、用户名和认证方式，并补充 SSH 端口与路由诊断。

## 0.4.4-alpha

- 修复 macOS 无法解析带版本号图标文件名、显示系统默认空白图标的问题。
- 使用完整 `.icns` 文件名并提升构建版本，避免 Launchpad 与 Dock 继续命中旧图标缓存。

## 0.4.3-alpha

- 上传文件改为批次进度模型，逐项展示等待、大小计算、上传中、完成与失败状态。
- 普通文件展示已传输字节、总大小和百分比；文件夹递归统计内容并轮询远程实际写入大小。
- 多文件上传开始时自动显示原生进度列表，完成后保留成功数量、失败数量及逐项错误详情。
- 上传完成或部分失败后自动刷新当前远程目录，失败项目不会阻止后续项目继续上传。
- ad-hoc 签名增加稳定的 `com.kiteshell.app` 指定要求，避免每次二进制更新因 CDHash 改变而被 macOS 识别成新应用。
- 保留版本化图标资源与固定 Bundle ID；正式分发时可无缝替换为固定 Apple Developer ID 签名。

## 0.4.2-alpha

- 远程普通文件支持双击下载到 KiteShell 专用缓存，并使用 macOS 默认应用打开预览或编辑。
- 监控本地编辑副本的大小、修改时间和 inode，兼容原地保存与原子替换保存方式。
- 本地保存后通过当前 SSH ControlMaster 会话自动覆盖上传原远程路径，无需再次认证。
- 自动同步失败时保留本地副本、显示明确状态并每 3 秒重试；SSH 恢复后自动补传。
- 远程文件右键菜单增加“下载并打开编辑”，同时保留单纯“下载到…”操作。
- 增加隔离 SFTP 编辑回环测试，真实验证下载、本地修改及覆盖上传链路。

## 0.4.1-alpha

- 使用版本化图标资源名，强制 LaunchServices 与启动台重新读取应用图标，避免同 Bundle ID 图标缓存。
- 修复连接中心搜索框启动时自动聚焦；支持按 Esc 或点击列表空白区域退出输入状态。
- 为服务器卡片、标签、目录、命令和主要按钮补充悬停反馈、帮助提示与指针光标。
- 工作区“＋”改为连接选择器，可直接从已保存服务器打开会话，也可新建服务器并立即连接。
- 压缩服务器信息面板的卡片、区块、进度条和进程行间距，提高单位面积信息量。
- 将命令和脚本管理从连接表单移至会话底部工作区，支持新建、编辑、删除、导入本地脚本与一键运行。
- 远程文件路径支持双击编辑并跳转；空白处和文件右键菜单增加前往目录、终端进入、粘贴上传、新建文件、创建副本和权限修改等有效操作。

## 0.4.0-alpha

- 重做连接中心列表为原生卡片布局，补充连接状态、分组、认证方式、快捷命令数量和最近连接时间。
- 将 FinalShell 导入入口移到列表标题区右侧并使用上传语义；活动会话可从连接中心一键返回。
- 工作区和会话标签栏均可直接新建连接，无需先返回连接列表。
- 增加六套终端主题，覆盖背景、前景、光标、选区和完整 16 色 ANSI 调色板，运行中的终端可即时切换。
- 每台服务器可保存多条命令或脚本，并可在连接列表和终端中一键执行。
- 接入 Shell OSC 7 当前目录上报；终端执行 `cd` 后，远程文件工作区自动跟随相同目录。

## 0.3.1-alpha

- 在连接中心侧边栏和设置“关于”区域显示当前版本号与构建号。
- 重做服务器监控视觉层级，为 CPU、内存、交换空间和磁盘增加彩色百分比进度条。
- 高负载进程同时显示 CPU 与内存占用百分比及独立进度条，并用颜色区分资源类型。
- 远程文件支持从 Finder 直接拖入当前目录上传，拖入时显示明确目标反馈。
- 完善远程文件右键菜单：打开、下载、复制路径、上传、新建文件夹、重命名和删除。
- 重新设计应用图标，以服务器机架、SSH 连接和终端提示符直接表达产品用途。

## 0.3.0-alpha

- 密码可选择持久化保存到 macOS Keychain，并支持清除或重新输入已保存密码。
- 修复 AskPass 辅助程序权限和首次主机指纹提示占用密码输入的问题；新增隔离 SSH 服务端到端密码认证测试。
- 增加跟随系统、浅色、深色三种外观模式，以及预设和自定义强调色。
- 连接标签状态点使用绿色、橙色和红色区分已连接、连接中和断开/失败。
- 修复连接中心分组行无法点击筛选的问题。
- 接通真实 Linux 运行时间、负载、CPU、内存、交换空间、网络、磁盘和进程监控。
- 接通真实远程目录浏览，并支持上传、下载、新建文件夹、重命名和删除。
- 监控和远程文件复用当前 SSH ControlMaster 会话，不重复认证；解析任务移出主线程。
- 连接失败信息改为一处中性状态文字，监控和文件面板不再重复堆叠红色错误。
- 增加不依赖 XCTest 的核心自测脚本，覆盖解析器、FinalShell 导入、AskPass 和 Keychain。

## 0.2.2-alpha

- 修复空连接中心内容未撑满窗口导致的纵向错位。
- 密码认证连接在启动前显示原生密码输入框，密码仅用于当前认证。
- 增加连接编辑、收藏和删除菜单，以及删除确认。
- 删除连接时同步关闭其活动会话。
- 连接未成功时仅显示终端，隐藏监控与文件面板，减少重复错误提示。
- 失败标签改用中性状态点，只在终端标题区保留一处错误状态。

## 0.2.1-alpha

- 增加 KiteShell 原创 macOS 应用图标。
- 图标使用风筝与终端提示符组合，完整提供 16–1024 像素尺寸。

## 0.2.0-alpha

- 产品更名为 KiteShell，应用、可执行文件、包标识和安装包同步更新。
- 首次启动不再生成任何服务器、监控、进程、网络、磁盘或文件示例数据。
- 支持导入 FinalShell JSON 连接配置，包括单条、数组和嵌套导出结构。
- 按用户名、主机和端口自动跳过重复连接。
- FinalShell 私有加密密码不会被误当成可用凭据；导入后首次连接重新认证。

## 0.1.1-alpha

- 移除所有内置示例服务器、模拟系统指标、模拟进程和模拟文件列表。
- SSH 状态改为由认证成功事件驱动，不再在启动进程后直接标记为已连接。
- 连接超时、认证失败和 SSH 进程退出时显示真实失败或断开状态。
- 仅在真实连接成功后更新最近连接时间。
- 监控与 SFTP 尚未接入时显示明确的不可用状态。
- 自动清理由 0.1.0-alpha 创建的三个文档示例服务器。
