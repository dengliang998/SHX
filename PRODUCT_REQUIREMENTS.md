# KiteShell 产品需求与发展规划

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | KiteShell |
| 文档版本 | V1.4 |
| 基线版本 | 0.5.3-alpha（Build 21） |
| 更新日期 | 2026-07-27 |
| 产品形态 | macOS 原生桌面应用 |
| 支持平台 | macOS 14 及以上、Apple Silicon |
| 目标用户 | 开发者、运维人员、站长、小型技术团队 |
| 核心定位 | 将 SSH 终端、远程文件、服务器监控和常用运维操作整合为一个流畅、安全的 macOS 工作台 |

本文档是后续产品设计、研发排期和版本验收的主需求文档。原始需求文档仍作为设计原则和长期愿景参考；本版重点解决以下问题：

1. 明确当前产品已经能做什么，避免重复开发或将规划功能误认为已完成。
2. 将影响日常使用的基础能力优先补齐，而不是继续堆叠不稳定功能。
3. 把后续内容按 P0、P1、P2 分级，并给出可执行的版本路线图。
4. 为关键功能提供用户流程和验收标准，便于开发后进行整体回归测试。

## 2. 产品定义

### 2.1 产品愿景

让 Mac 用户不需要在终端、SFTP 客户端、系统监控和脚本工具之间反复切换，即可安全、清晰地完成服务器日常维护。

KiteShell 不以“功能最多”为目标，而以以下体验为核心：

- 连接可靠：内网、外网、密码、密钥等常见场景都能快速连接，错误可理解、可处理。
- 操作连贯：连接、终端、文件、监控和脚本围绕同一个服务器会话协同工作。
- 数据真实：只展示从真实服务器获取的数据，不使用模拟负载、文件或连接状态。
- 符合 macOS：使用原生窗口、菜单、快捷键、拖拽、本地数据目录和系统反馈。
- 安全可信：凭据不明文存储，敏感信息不进入日志，危险操作有明确保护。
- 长期流畅：终端输出、文件传输和监控采集不阻塞主线程，不随使用时长逐渐卡顿。

### 2.2 产品边界

当前产品只面向 macOS，不兼容 Windows 或 Linux 桌面系统，不采用 Electron、Flutter、Qt 或 WebView 承载主界面。

现阶段不进入以下方向：

- RDP、VNC 等远程桌面。
- 企业堡垒机、多人审批和完整审计平台。
- Kubernetes、云资源计费或 CMDB 管理平台。
- 自动替用户执行高风险运维决策。
- 默认上传终端命令、服务器地址、文件路径或文件内容。

## 3. 用户与核心场景

### 3.1 用户角色

| 用户 | 典型任务 | 最关注的问题 |
|---|---|---|
| 个人开发者 | 部署应用、查看日志、修改配置、重启服务 | 快速、简单，不频繁输入密码 |
| 运维人员 | 管理多台服务器、排查负载、传输文件、执行脚本 | 稳定、多会话、错误诊断和操作安全 |
| 站长 | 更新网站文件、证书和配置、下载备份 | 文件操作直观，上传下载可追踪 |
| 小型团队成员 | 使用统一连接命名和常用脚本 | 配置可导入导出，敏感信息边界清晰 |

### 3.2 高频用户流程

#### A. 首次使用

启动应用 → 看到空状态引导 → 新建或导入连接 → 输入并选择是否本地保存密码 → 测试连接 → 进入终端；首次主机密钥由 OpenSSH 后台记录。

#### B. 日常连接

启动应用 → 从最近连接、收藏或分组定位服务器 → 双击或按回车连接 → 自动读取保存的凭据 → 恢复该服务器上次的工作区布局。

#### C. 修改远程文件

连接服务器 → 文件面板跟随终端当前目录 → 双击文件下载并使用本地应用打开 → 本地保存 → 自动上传覆盖原文件 → 成功或失败状态明确可见。

#### D. 部署文件

连接服务器 → 进入目标目录 → 从 Finder 拖入多个文件 → 查看逐文件进度、速度和结果 → 失败项重试 → 刷新目录确认结果。

#### E. 排查服务器异常

连接服务器 → 打开服务器信息 → 查看 CPU、内存、磁盘和高负载进程 → 复制诊断信息或进入终端执行进一步检查。

#### F. 执行常用任务

连接服务器 → 打开命令与脚本 → 选择预设任务 → 检查或填写变量 → 在当前终端执行 → 查看输出，不重复创建连接。

## 4. 当前版本能力盘点

状态说明：`已实现` 表示当前版本已有完整入口；`部分实现` 表示核心链路已存在但仍缺少专业能力；`未实现` 表示仅在旧需求中规划。

| 模块 | 当前状态 | 已有能力 | 主要缺口 |
|---|---|---|---|
| macOS 原生应用 | 已实现 | SwiftUI + AppKit、Apple Silicon、原生窗口/菜单/设置、原创图标 | 正式 Developer ID、公证、Hardened Runtime、在线自动更新 |
| 连接中心 | 已实现 | 新建、编辑、删除、复制、收藏、名称/时间/状态排序、搜索、快速连接、持久化空分组、拖放移动 | 多选批量操作、独立窗口打开、完整撤销 |
| FinalShell 导入 | 部分实现 | JSON 与嵌套结构识别、去重、Java 兼容密码解析、转存本地凭据库、分类结果提示 | 导入预览、逐项选择、字段映射、完整保留原始多级文件夹 |
| KiteShell 导入导出 | 已实现 | Schema 2 无凭据 JSON，包含连接、空分组、标签、备注、连接命令和全局命令 | 加密完整备份、逐项预览、覆盖/保留两者冲突策略 |
| 密码认证 | 已实现 | 新建/编辑页直接输入、保存并连接、本地 AES-GCM 凭据库、状态查看、清除/重新输入，不调用 Keychain | 可选主密码保护、正式签名后的跨版本迁移验证 |
| 私钥认证 | 部分实现 | 指定私钥、Security-Scoped Bookmark、加密私钥识别、口令本地加密持久化 | 密钥生成、部署公钥、完整权限修复工具 |
| SSH Agent | 已实现 | Agent 可用性、已加载身份数量和指纹列表、系统 OpenSSH 认证 | Agent 转发的独立风险设置 |
| SSH 终端 | 部分实现 | 真终端、UTF-8/ANSI/真彩色、多标签、搜索、自动重连、主题、字号、行距、光标、滚动历史、危险粘贴确认 | 分屏、标签拖出窗口、会话复制/固定、自定义快捷键 |
| 当前目录联动 | 已实现 | Shell Integration + OSC 7、文件面板随 `cd`、路径锁定与恢复跟随 | 非 Bash/Zsh Shell 专项兼容配置 |
| 服务器监控 | 部分实现 | 真实 CPU、内存、Swap、负载、磁盘、网络实时速率、进程搜索/排序/SIGTERM | 历史趋势、阈值告警、完整 Linux 发行版矩阵 |
| 远程文件浏览 | 部分实现 | 进入/上级、路径编辑、前后历史、刷新、筛选、排序、隐藏文件 | 远程目录树、10 万项增量加载、收藏、批量选择 |
| 远程文件操作 | 部分实现 | 上传、下载、文件夹下载、新建、重命名、复制、移动、权限、删除、拖放和右键菜单 | Quick Look、完整批量操作、批次覆盖策略 |
| 内置/外部编辑 | 已实现 | 内置 UTF-8 编辑器、LF/CRLF、查找计数、保存上传；外部编辑监测、失败重试、远端版本冲突阻断 | 差异比较、缓存版本恢复、更多文本编码 |
| 文件传输中心 | 部分实现 | 跨会话任务、上传逐项/总进度、速度、剩余时间、暂停/继续、取消、失败重试、通知 | 下载逐字节进度、单项并发控制、断线断点续传 |
| 命令与脚本 | 部分实现 | 连接/全局库、搜索、标签、变量、三种执行模式、高风险确认、最近执行元数据 | 文件夹、收藏、上传临时脚本后执行、可靠退出状态采集 |
| 主题设置 | 部分实现 | 系统/浅色/深色、强调色、六套终端主题、字号、行距、光标和滚动历史 | 自定义 ANSI 调色板、主题导入导出、按服务器覆盖主题 |
| 工作区 | 部分实现 | 多标签、标签拖动排序、监控/文件/脚本面板、专注终端、布局持久化、安全恢复 | 分屏、多窗口、标签拖出和窗口间移动 |
| 主机密钥 | 已实现 | 首次由 OpenSSH `accept-new` 后台记录、不展示确认窗口、变化阻断、含空格路径校验 | 可选的严格人工核对模式、正式发布环境长期升级回归 |
| 跳板机/代理 | 部分实现 | 单级 ProxyJump、OpenSSH 主机密钥保护、无认证 SOCKS5/HTTP CONNECT | 多级跳板、带凭据代理安全隧道 |
| 端口转发 | 已实现 | 本地、远程、动态 SOCKS5，保存、启停、ControlMaster 即时控制、开放监听风险提示 | 独立于终端会话的长期转发守护 |
| 日志与诊断 | 已实现 | 结构化连接诊断、地址端口测试、脱敏日志、诊断报告导出、日志查看/清理 | 自动崩溃报告收集与反馈服务 |
| 会话恢复 | 已实现 | 恢复标签、选中会话、面板布局和远程路径入口，不自动登录或重放命令 | 普通 Shell 进程级恢复不在安全边界内 |

### 4.1 V1.4 基线验证结果

- Swift 单元与模型测试：28 项全部通过。
- 核心自测：6/6 通过。
- 隔离密码 SSH：真实系统 OpenSSH、AskPass 和 PTY 链路通过。
- 隔离 SFTP：ControlMaster 下载、本地修改、覆盖上传和内容校验通过。
- 本地凭据：AES-GCM 加密往返、密文不含明文密码、目录 `700` 与文件 `600` 权限验证通过。
- FinalShell 密码：Java 17 原算法和 Swift 解码器对同一参考密文得到一致结果，嵌套导入后凭据能按连接 ID 关联。
- 主机密钥：首次由 OpenSSH `accept-new` 静默记录，已记录密钥变化时仍阻止连接。
- 连接前性能：不再执行阻塞式 `ssh-keyscan` 或展示指纹确认窗口；此前公网服务器前置扫描实测约 2.36 秒。
- Release App 与 DMG 内 App：固定 `KiteShell Local Code Signing` 身份签名，`codesign --deep --strict` 通过。
- 当前本机签名只用于开发测试；未经 Developer ID 签名和 Apple 公证，因此不视为正式公网发布版本。

## 5. 产品设计与交互原则

### 5.1 真实状态优先

- 服务器未连接时不展示负载、进程和文件等虚假数据。
- 连接状态由真实认证结果驱动，不能在 SSH 进程刚启动时提前显示成功。
- 同一个错误只在最合适的位置展示一次，其他区域使用中性不可用状态。
- 长任务必须展示进行中、完成、失败或取消，不允许无反馈等待。

### 5.2 操作闭环

- 用户在工作区中可以直接选择已有连接或新增连接，不必返回列表。
- 从连接中心进入工作区后，可以随时返回连接中心，也能一键回到已有会话。
- 新建连接后可以选择“仅保存”“保存并连接”；临时连接成功后可以“保存为连接”。
- 文件、监控和脚本必须使用当前会话，避免重复认证。

### 5.3 macOS 熟悉感

- 使用原生 Toolbar、Sidebar、Inspector、Table、Split View、Sheet 和菜单。
- 所有图标按钮提供 Tooltip 和辅助功能描述；可点击项有悬停和指针反馈。
- 支持标准快捷键、右键菜单、Finder 拖放、系统文件选择器和系统通知。
- 点击空白区域或按 Esc 可以取消搜索框、路径框等输入焦点。
- 动效克制、可中断，并响应“减少动态效果”和“减少透明度”。

### 5.4 渐进式复杂度

- 新建连接首屏只展示名称、主机、端口、用户和认证方式。
- 私钥、跳板机、代理、算法、心跳等放入“高级设置”。
- 常用操作直接可见，低频操作放入上下文菜单或更多菜单。
- 错误先给用户能理解的原因和处理建议，技术详情默认折叠并可复制。

## 6. 功能需求

### 6.1 首次启动与新手引导

优先级：P0

需求：

- 无连接时展示三个明确入口：新建连接、导入 FinalShell、导入 KiteShell 备份。
- 首次连接前检查本地网络权限、本地凭据目录和系统 SSH 可用性，只在必要时引导。
- 对密码、私钥和 SSH Agent 给出简短选择说明。
- 首次成功连接后以非阻塞方式提示文件面板、监控和脚本入口。
- 提供“打开示例说明”，但不得创建虚假服务器或模拟数据。

验收标准：

- 新用户从启动到创建连接不超过 3 个主要决策步骤。
- 任何权限请求都在用户触发相关功能后出现，并说明用途。
- 取消引导不影响用户直接使用应用。

### 6.2 连接中心

优先级：P0

需求：

- 支持全部、收藏、最近连接和分组筛选。
- 支持按名称、主机、用户、分组、标签和备注搜索。
- 支持按名称、最近连接时间、创建时间和连接状态排序。
- 支持复制连接、编辑、删除、收藏、移动分组和多选批量移动。
- 支持拖动连接到分组；空分组仍可显示、重命名和删除。
- 卡片或列表明确展示名称、地址、认证方式、分组、上次连接结果和最近连接时间。
- 连接操作支持双击、回车、右键在新标签打开、在新窗口打开。
- 顶部导入按钮使用“上传/导入”语义并位于标题操作区，不放在内容中间。
- 有活动会话时展示“返回会话”入口。

验收标准：

- 1000 条连接时搜索输入和滚动保持流畅。
- 删除连接必须确认对象名称，并说明是否同时删除保存的凭据。
- 清除最近记录不得删除连接配置。
- 搜索框启动时不自动聚焦，点击空白处或按 Esc 可取消焦点。

### 6.3 新建、编辑与测试连接

优先级：P0

基础字段：

- 名称、主机、端口、用户名、认证方式和分组。
- 标签、备注、连接超时、心跳间隔、字符编码和启动目录。
- 可选的连接后初始化命令，默认不启用并提示风险。

认证方式：

- 密码：输入、显示/隐藏、记住密码、清除密码。
- 私钥：选择文件、私钥口令、权限和格式检查。
- SSH Agent：显示 Agent 是否可用和已加载身份数量。
- 使用系统 OpenSSH 配置：选择 Host 条目并继承 HostName、User、Port、IdentityFile、ProxyJump 等字段。

连接测试：

- 保存前提供“测试连接”，分阶段展示 DNS、TCP 和认证结果。
- 测试成功后可直接保存并连接。
- 测试失败显示分类错误和下一步建议，不堆叠多个红色错误。

验收标准：

- 主机为空、端口越界、用户名为空时不能保存。
- 编辑连接不改变其 ID，已保存凭据仍能正确关联。
- 更换认证方式时明确询问是否清除旧凭据。
- 内网地址在授权完成后不重复等待本地网络探测。

### 6.4 凭据与主机信任

优先级：P0

需求：

- 密码和私钥口令存入当前用户的本地 AES-GCM 加密凭据库，普通配置仅保存连接 ID。
- 凭据目录权限为 `700`，密钥和密文文件权限为 `600`，应用不得调用系统钥匙串。
- 用户可以在连接编辑页看到“已保存密码”“未保存”“需要重新输入”等状态。
- 首次主机密钥由系统 OpenSSH 使用 `accept-new` 后台记录，不展示确认弹窗。
- 已记录主机密钥发生变化时必须阻止连接，不得静默覆盖。

验收标准：

- 日志、导出文件和崩溃信息中不得出现密码、口令或私钥内容。
- 更新应用或切换连接时不得出现钥匙串授权弹框。
- 删除连接后清理对应本地凭据项。

### 6.5 SSH 终端

优先级：P0/P1

P0：

- 支持 UTF-8、ANSI/VT、256 色和真彩色。
- 支持中文输入法、复制、粘贴、选择、链接点击、清屏和 Retina 渲染。
- 支持标签页、多会话、窗口缩放同步 PTY 和保持终端缓冲区。
- 状态点使用绿色表示已连接、橙色表示连接中/重连中、红色表示断开/失败，并同时提供文字或辅助描述。
- 失败后保留终端内容，提供重试、重新输入密码和复制错误详情。
- 提供可配置自动重连：关闭、1 次、3 次或持续重试；采用退避时间。
- 心跳失败后及时更新状态，不把已退出的进程显示为在线。

P1：

- 终端内容搜索，支持上一个/下一个匹配。
- 水平/垂直分屏，同一服务器可创建多个 Shell。
- 标签拖动排序、拖出新窗口和窗口间移动。
- 复制会话、重命名标签、固定标签。
- 可配置滚动历史、光标样式、行距、Option 键行为和快捷键。
- 粘贴多行或包含高风险命令时可选安全提示。

验收标准：

- `vim`、`top`、`htop`、`tmux`、`less` 等常用程序显示和操作正常。
- 连续高速输出时终端输入仍可响应，不出现持续主线程卡顿。
- 切换标签不重建终端或丢失滚动位置。
- 自动重连不得自动重放上一次命令。

### 6.6 工作区与导航

优先级：P0

需求：

- 工作区由会话标签、终端、服务器检查器、文件/脚本底部面板和状态区组成。
- “＋”打开已有连接选择器，同时提供“新建连接并打开”。
- 终端获得剩余空间最高优先级；窗口变小时自动压缩或收起辅助面板。
- 监控和文件区域始终跟随当前会话，不显示上一个服务器的数据。
- 检查器宽度、底部面板高度、显示状态和选中子页面可记忆。
- 支持专注终端模式，一键隐藏所有辅助面板。
- 启动时可选择打开连接中心或恢复上次工作区。
- 崩溃恢复只恢复连接入口和布局，不自动执行命令或恢复密码输入。

验收标准：

- 在连接中心、已有会话和新建连接之间能形成完整导航闭环。
- 隐藏面板后，进行中的上传和监控任务按规则继续；再次显示时状态一致。
- 切换 10 个会话不发生监控、目录或进度串页。

### 6.7 远程文件浏览

优先级：P0/P1

P0：

- 展示名称、大小、修改时间、权限和所有者。
- 支持进入目录、返回上级、刷新、直接编辑路径和跳转。
- 终端执行 `cd` 后文件面板自动跟随；用户可锁定文件面板路径停止跟随。
- 支持按名称、大小、修改时间和类型排序。
- 支持显示/隐藏点文件。
- 支持在当前目录搜索，搜索不得阻塞界面。
- 刷新后尽量保留选择、排序和滚动位置。

P1：

- 前进/后退历史。
- 按需加载的远程目录树。
- 大目录增量加载或虚拟化列表。
- 收藏远程路径和每台服务器独立的最近目录。

验收标准：

- 双击目录进入，双击普通文件执行用户设定的预览/编辑动作。
- 路径不存在或权限不足时保留当前可用目录，并显示可恢复错误。
- 10 万项极端目录不能一次性阻塞主线程渲染。

### 6.8 远程文件操作与编辑

优先级：P0/P1

P0：

- 上传、下载、新建文件/文件夹、重命名、复制、移动、删除和权限修改。
- Finder 文件可拖入当前远程目录，多文件批量上传。
- 文件和空白区域提供符合上下文的右键菜单。
- 支持复制名称、远程绝对路径和 Shell 安全转义路径。
- 支持“在终端进入此目录”。
- 删除、覆盖和权限修改必须明确目标对象及风险。

P1：

- Quick Look 预览常见文件。
- 内置文本编辑器支持编码、换行符、查找、只读提示和保存上传。
- 文件夹递归下载。
- 批量选择、批量下载、批量删除和批量权限修改。
- 冲突策略：询问、覆盖、跳过、保留两者，并支持“应用于本批次”。
- 外部编辑回传前比较远程修改时间或哈希，避免覆盖他人的新版本。
- 同步失败时允许立即重试、另存本地副本、查看详情或停止监控。

验收标准：

- 本地编辑保存后只上传目标文件，不刷新或重传无关文件。
- 网络中断时保留本地编辑副本，恢复后由用户策略决定自动或手动补传。
- 可执行文件双击不得直接运行。

### 6.9 文件传输中心

优先级：P0

需求：

- 所有上传、下载和编辑回传进入统一任务模型。
- 展示服务器、方向、文件名、源/目标路径、总大小、已传输、百分比、速度、剩余时间和状态。
- 支持等待、进行中、暂停、完成、失败和取消。
- 支持暂停/继续、取消单项、取消全部、失败重试和清除已完成。
- 可配置全局和单服务器并发数，默认避免占满 SSH 连接。
- 完成或失败时可使用系统通知；应用在前台时避免重复通知。
- 退出应用时提示仍在进行的任务。
- 后续支持断点续传；首版无法续传时必须明确说明会重新开始。

验收标准：

- 多文件任务中单个失败不阻止其他文件继续。
- 2 GB 单文件传输完成后大小一致；可选进行 SHA256 校验。
- 传输大文件时终端输入、滚动和监控仍可使用。
- 用户能够定位失败的具体文件和服务器，而不是只看到“上传失败”。

### 6.10 服务器监控与进程

优先级：P1

需求：

- 展示运行时间、负载、CPU、内存、Swap、磁盘、网络速率和高负载进程。
- CPU、内存、Swap 和磁盘同时展示数值、百分比和颜色进度，不只依赖颜色。
- 网络从累计流量扩展为实时上/下行速率，并展示短时趋势。
- 进程展示 PID、用户、命令、CPU 和内存；支持搜索及按 CPU/内存排序。
- 进程详情可复制启动命令；结束进程属于高级危险操作，需明确权限、信号和确认。
- 模块可折叠，并记住每台服务器的展开状态。
- 监控采样周期可设置；检查器隐藏或应用后台时自动降频。
- 命令不存在、权限不足或系统不兼容时只影响对应模块。

验收标准：

- Ubuntu、Debian、CentOS Stream、Rocky Linux 和 Alpine Linux 完成兼容验证。
- 上一轮采集未完成时不堆积下一轮任务。
- 断线后停止采集，重连后自动恢复。
- 监控错误不影响终端和文件功能。

### 6.11 命令与脚本库

优先级：P1

需求：

- 命令和脚本在连接服务器后可随时新增、编辑、删除、导入和执行。
- 每个连接拥有私有库，同时提供全局通用库。
- 支持文件夹、标签、搜索、收藏和最近执行。
- 支持变量，例如 `${environment}`、`${service}`，执行前以表单收集值。
- 支持三种执行方式：插入终端不执行、确认后执行、直接执行。
- 包含换行、`sudo`、删除或重启等高风险内容时默认要求确认。
- 脚本可以上传到临时路径后执行，结束后按策略清理。
- 展示执行时间和退出状态；默认不持久化完整输出。

验收标准：

- 新增脚本不需要返回连接编辑页。
- 执行使用当前已认证会话，不重复索取密码。
- 用户可以在执行前查看最终替换变量后的内容。

### 6.12 导入、导出与备份

优先级：P0/P1

P0：

- 导出不含凭据的 KiteShell JSON，包含连接、分组、标签、备注和命令脚本。
- 导入前展示预览，支持选择条目和冲突策略：跳过、覆盖、保留两者。
- FinalShell 导入解析兼容格式的加密密码并转存本地凭据库，展示识别、重复、保存成功、解析失败和需重新输入密码的数量。
- 支持从 `~/.ssh/config` 导入可识别的 Host 配置。

P1：

- 提供使用独立密码加密的完整备份，可选择包含凭据。
- 支持定期本地备份及恢复前检查。
- 导出格式带 `schemaVersion`，后续版本可迁移。

验收标准：

- 普通导出文件不得包含密码、私钥口令或本地凭据库数据。
- 导入失败不得破坏已有连接。
- 导入同一文件两次不会无提示地产生大量重复项。

### 6.13 跳板机、代理与端口转发

优先级：P1

跳板机：

- 支持引用已保存连接作为 ProxyJump。
- 支持目标服务器和跳板机分别认证及校验指纹。
- 首期支持单级跳板，后续扩展多级。

代理：

- 支持 SOCKS5 和 HTTP CONNECT 代理。
- 代理凭据与服务器凭据分开存储。

端口转发：

- 支持本地转发、远程转发和动态 SOCKS5。
- 每个转发可命名、保存、启动、停止并显示监听状态。
- 监听 `0.0.0.0` 或所有网卡时提示安全风险。
- 端口占用、远端禁止转发和网络中断时显示明确原因。

验收标准：

- 转发关闭或会话断开后释放本地端口和子进程。
- 应用退出前提示仍在运行的转发。
- 状态显示必须来自真实监听和 SSH 进程状态。

### 6.14 设置、主题与个性化

优先级：P1

需求：

- 外观支持跟随系统、浅色、深色和自定义强调色。
- 内置多套终端主题，覆盖背景、文字、光标、选区和 ANSI 颜色。
- 支持自定义终端主题及导入/导出。
- 设置终端字体、字号、行距、光标和滚动历史。
- 设置默认启动页面、默认面板布局、监控周期、传输并发和通知。
- 支持按服务器覆盖终端主题和启动目录。
- 关于页展示版本、Build、签名类型、开源许可和检查更新。

验收标准：

- 主题切换立即应用于正在运行的终端，不重连会话。
- 深浅色下检查所有文字、状态点和进度条对比度。
- 自定义颜色非法时恢复最近一次有效值。

### 6.15 错误、诊断与用户支持

优先级：P0

需求：

- 将错误分类为 DNS、路由、超时、拒绝连接、认证、主机指纹、算法、权限、磁盘空间和本地文件错误。
- 每个错误包含：简短结论、可能原因、建议操作、可复制技术详情。
- 提供“连接诊断”，依次检查 DNS、端口、本地网络权限、SSH 版本和认证阶段。
- 设置中提供诊断日志开关、查看日志目录和导出诊断包。
- 日志默认不记录终端输入输出、密码、私钥或远程文件内容。
- 诊断包导出前列出内容并完成脱敏。
- 提供版本说明、反馈入口和崩溃后恢复提示。

验收标准：

- 同一错误在一个界面只重点展示一次。
- 普通用户不需要阅读原始 OpenSSH 输出即可知道下一步。
- 技术详情可复制，便于向管理员或开发者求助。

### 6.16 更新、安装与数据迁移

优先级：P0（正式发布前）

需求：

- 正式分发使用 Developer ID Application 签名、公证和 Hardened Runtime。
- 支持应用内检查更新、查看版本说明、下载和安全安装。
- 更新包必须验证签名、版本和完整性。
- 更新不得覆盖连接配置、本地凭据、脚本、主题和布局。
- 配置结构变化时执行可回滚迁移；迁移前创建本地备份。
- 提供清晰卸载说明，并区分应用、普通配置、缓存和本地凭据。

验收标准：

- 从上一正式版本升级后连接和凭据仍可使用。
- 更新后凭据仍可读取，且不触发系统钥匙串授权。
- 安装失败可继续使用旧版本，不留下半更新状态。

## 7. 便于用户使用的新增需求

以下内容不是简单增加功能，而是缩短用户完成任务的路径。

### 7.1 快速连接

优先级：P1

- 使用 `user@host:port` 快速打开临时会话，不强制保存。
- 连接成功后可一键保存为正式连接。
- 支持从剪贴板识别 SSH 地址，但只提示，不自动连接。

### 7.2 命令面板

优先级：P1

- 使用统一快捷键打开命令面板，搜索服务器、会话、命令、远程路径和应用操作。
- 支持键盘完成“搜索服务器 → 连接 → 运行命令”等高频流程。
- 最近项目优先，但不能把敏感内容写入搜索索引。

### 7.3 收藏与最近项目

优先级：P1

- 收藏服务器、脚本和远程目录。
- 最近连接显示上次结果；最近目录按服务器隔离。
- 用户可以清除历史而不删除配置。

### 7.4 操作撤销与恢复

优先级：P1

- 连接移动分组、重命名和收藏操作支持撤销。
- 删除远程文件由于协议限制通常无法撤销，必须确认并鼓励使用备份。
- 本地编辑回传前保留有限数量的临时版本，允许在应用缓存中恢复。

### 7.5 预检与环境提示

优先级：P1

- 上传前检查目标目录权限和可用空间（服务器支持时）。
- 执行脚本前检查依赖命令和当前用户权限。
- 端口转发前检查本地端口占用。
- 风险提示只在有实际风险时出现，不滥用确认框。

### 7.6 tmux 辅助

优先级：P2

- 检测服务器是否安装 tmux。
- 支持创建、列出和重新附加 tmux 会话。
- 断线重连后提示恢复已有 tmux，而不是声称恢复普通 SSH Shell。

### 7.7 可选通知

优先级：P1

- 大文件传输完成、脚本执行完成、连接最终失败时可通知。
- 用户可按事件类型和服务器关闭通知。
- 前台已经能看到结果时不重复弹出系统通知。

### 7.8 帮助与上下文说明

优先级：P1

- 所有不明确的图标按钮提供 Tooltip。
- 错误页提供对应帮助主题，而不是跳转到泛化首页。
- 提供“连接不上怎么办”“如何使用密钥”“为什么主机指纹变化”等内置短说明。

## 8. 后续可扩展方向

### 8.1 本地优先的跨设备同步（P2）

- 使用 iCloud 同步非敏感连接配置、分组、脚本和主题。
- 凭据只有在明确启用并完成端到端加密后才允许同步。
- 冲突时保留两份并让用户选择，不静默覆盖。

### 8.2 监控历史与轻量告警（P2）

- 保存可配置时长的本地短期趋势，不建设集中式监控平台。
- CPU、内存、磁盘超过阈值持续一段时间后本地通知。
- 默认关闭历史采集，说明本地存储占用。

### 8.3 连接模板（P2）

- 为同一环境批量创建连接，统一默认用户、端口、跳板机和脚本。
- 模板变化不自动覆盖已有连接，避免意外修改。

### 8.4 密钥工具（P2）

- 生成 Ed25519 密钥、复制公钥、检查权限和部署 `authorized_keys`。
- 私钥默认保存到用户明确选择的位置，不内置云端托管。

### 8.5 AI 辅助（P3，需单独立项）

- 可以解释错误、生成命令草稿或总结用户主动选择的日志。
- 默认不发送终端上下文、主机信息和文件内容。
- 执行前必须由用户确认，绝不自动运行生成命令。
- 需要单独完成隐私、成本、模型选择和离线替代方案评审。

### 8.6 团队能力（P3，需单独产品线评估）

- 团队连接目录、角色权限、共享脚本和审计。
- 凭据托管、审批和堡垒机不应直接叠加在个人版数据模型上。

## 9. 安全与隐私要求

1. 密码和口令只存储在权限受限的本地 AES-GCM 凭据库，私钥内容不进入应用配置。
2. 首次主机密钥可由 OpenSSH 静默记录，但已记录密钥的变化不得静默接受。
3. 日志、导出、诊断包和崩溃信息统一执行敏感字段脱敏。
4. 监控只执行预定义只读命令；用户输入作为参数时必须安全转义。
5. 路径、文件名和脚本变量不得未经处理直接拼接到 Shell 命令。
6. 删除、覆盖、结束进程、开放监听和执行高风险脚本必须明确确认。
7. 默认不采集终端命令、终端输出、服务器地址和文件内容。
8. 临时下载和外部编辑缓存提供自动清理周期及手动清理入口。
9. 正式发布前完成依赖许可证、漏洞和供应链检查。
10. 本机自签名身份仅用于当前开发测试，不能替代正式 Developer ID 分发。
11. 本地凭据密钥与密文均属于当前 macOS 用户，不宣称具备 Keychain 或安全隔区级别的同账户攻击防护。

## 10. 非功能需求

### 10.1 性能

- 基线设备：Apple M1、8 GB 内存、最低受支持 macOS。
- 冷启动 P50 不超过 1.2 秒，P95 不超过 2 秒。
- 本地点击新建会话到标签出现 P95 不超过 100 毫秒，网络耗时单独计算。
- 终端本地输入到渲染任务提交 P95 不超过 16 毫秒。
- 10 个普通 SSH 会话下切换、输入和滚动保持流畅。
- 空闲无连接 CPU 平均低于 1%；后台隐藏的监控自动降频。
- 空闲常驻内存目标低于 150 MB，10 个普通会话目标低于 600 MB。
- 大目录解析、监控解析和文件传输不得在主线程执行。

### 10.2 稳定性

- SSH、SFTP、监控或单个终端异常不导致整个应用退出。
- 长任务支持取消、超时和资源释放。
- 连续运行 24 小时无明显持续内存增长。
- 连接中断、睡眠唤醒、网络切换后状态能够正确恢复或明确失败。
- 关闭标签、窗口和应用时清理 SSH ControlMaster、临时 Socket、任务和观察器。

### 10.3 可访问性

- 状态不能只依赖红绿颜色，提供图标、文字和 VoiceOver 描述。
- 核心流程支持全键盘控制和清晰焦点环。
- 响应减少动态效果、减少透明度、增强对比度和系统强调色。
- 允许合理放大界面字体和终端字号，不发生内容严重错位。

### 10.4 兼容性

- 仅支持 macOS 14 及以上、Apple Silicon。
- 服务端优先覆盖 OpenSSH 7.4 及以上。
- 重点验证 Ubuntu、Debian、CentOS Stream、Rocky Linux 和 Alpine Linux。
- 验证 IPv4、IPv6、域名、`.local`、局域网、VPN 和公网场景。

## 11. 数据模型补充

后续数据模型建议在现有 `ServerProfile` 和 `Session` 基础上补充：

### ServerProfile

- `schemaVersion`
- `identityFileBookmark`
- `privateKeyPassphraseReference`
- `connectionTimeout`
- `keepAliveInterval`
- `startupDirectory`
- `jumpHostID`
- `proxyConfigurationID`
- `terminalThemeOverride`
- `createdAt`、`updatedAt`

### SessionState

- `profileID`
- `customTitle`
- `lastRemotePath`
- `terminalWorkingDirectory`
- `inspectorVisible`
- `bottomPanelVisible`
- `bottomPanelSelection`
- `inspectorWidth`
- `bottomPanelHeight`
- `reconnectPolicy`

### TransferTask

- `sessionID`、`profileID`
- `direction`
- `sourcePath`、`targetPath`
- `totalBytes`、`transferredBytes`
- `bytesPerSecond`、`estimatedRemainingTime`
- `status`、`conflictPolicy`
- `errorCategory`、`errorDetail`
- `createdAt`、`startedAt`、`completedAt`

### CommandItem

- `scope`（全局/连接）
- `type`（命令/脚本）
- `folderID`、`tags`
- `content`
- `variables`
- `executionMode`
- `riskLevel`

所有结构化配置都必须带版本号并支持迁移；敏感值只保存在独立本地凭据库，不进入配置 JSON。

## 12. 版本路线图

### 0.5.0-alpha：基础易用性与诊断（当前已交付）

目标：让新用户能顺利创建、测试、连接并理解错误。

- 完善连接字段、指定私钥、测试连接。
- OpenSSH 首次主机密钥自动记录和变化阻断。
- 结构化连接错误与连接诊断。
- KiteShell 无凭据导入导出。
- 文件排序、隐藏文件、前后历史和路径锁定。
- 统一上传/下载任务模型，支持取消和失败重试。
- 会话与布局持久化第一版。
- 完整回归内网、密码、密钥、监控和文件流程。

### 0.6.0-alpha：专业连接能力

目标：覆盖开发者和运维人员常见复杂网络。

- OpenSSH Config 导入。
- SSH Agent 状态和私钥口令。
- 单级跳板机、SOCKS/HTTP 代理。
- 本地、远程、动态端口转发。
- 自动重连策略和睡眠/网络切换恢复。
- 终端搜索、标签管理和基础分屏。

### 0.7.0-beta：文件与自动化完善

目标：让文件管理和重复运维任务达到日常主力工具水平。

- 完整传输中心、速度、剩余时间、暂停/继续和并发控制。
- 文件夹下载、批量操作、冲突策略和 Quick Look。
- 外部编辑冲突检测与版本恢复。
- 全局/连接命令库、变量、分类和执行历史。
- 网络速率、进程搜索排序和监控兼容性增强。

### 0.8.0-beta：稳定性与 macOS 完成度

目标：从“功能可用”进入“长期稳定使用”。

- 工作区恢复、多窗口和标签拖拽。
- VoiceOver、全键盘、减少动态效果和多显示器验收。
- 性能、内存、能耗、24 小时长稳和大文件测试。
- 诊断包、崩溃恢复和用户帮助。
- 配置迁移、缓存清理和卸载说明。

### 1.0.0：正式发布

目标：提供可公开分发的稳定版本。

- Developer ID 签名、公证、Hardened Runtime。
- 自动更新和安全回滚。
- 隐私说明、开源许可证和版本说明。
- 支持系统/服务器测试矩阵全部通过。
- 无已知 P0/P1 阻断缺陷。

## 13. 测试矩阵

### 13.1 连接

- 公网 IPv4、内网 IPv4、IPv6、域名、`.local` 和 VPN。
- 密码、默认私钥、指定私钥、带口令私钥和 SSH Agent。
- 正确密码、错误密码、超时、拒绝、无路由、DNS 失败和主机指纹变化。
- 应用更新、重启 Mac、睡眠唤醒和网络切换后的凭据与连接行为。

### 13.2 终端

- 中文输入、复制粘贴、组合键、鼠标、链接和终端大小变化。
- `vim`、`nano`、`top`、`htop`、`tmux`、`less`。
- 大量输出、长行、彩色日志、10 会话和 24 小时长稳。

### 13.3 文件

- 空目录、无权限目录、超大目录、中文和特殊字符路径。
- 单文件、多文件、文件夹、2 GB 文件和大量小文件。
- 上传、下载、取消、失败、重试、覆盖冲突和网络中断。
- 外部编辑器原地保存、原子替换保存、断网保存和远端同时变化。

### 13.4 监控

- 各重点 Linux 发行版。
- 缺失 `free`、`df`、`ps` 等命令或权限受限场景。
- 多会话快速切换、后台降频和断线恢复。

### 13.5 macOS

- macOS 最低支持版本、当前正式版本和最新公开版本。
- 深色/浅色、Retina、多显示器、全屏、Stage Manager。
- VoiceOver、全键盘、减少动态效果、减少透明度和增强对比度。

## 14. 发布门禁

版本进入正式发布前必须满足：

1. 新建连接、SSH、监控、上传、下载和编辑回传主流程全部通过。
2. 密码和私钥口令加密存储，无明文落盘或钥匙串授权弹框。
3. 首次主机密钥静默记录和后续变化阻断正确。
4. 2 GB 文件、多文件失败隔离和网络中断场景通过。
5. 10 会话、高速终端输出和监控并行无严重卡顿。
6. 8 小时业务长稳及 24 小时空闲/普通连接长稳通过。
7. 应用升级不丢连接、凭据、脚本、主题或布局。
8. 所有 P0/P1 缺陷关闭，或有明确且不影响核心流程的发布评估。
9. Developer ID、公证、Gatekeeper 和更新签名验证通过。
10. 隐私说明、许可证、诊断和反馈入口准备完成。

## 15. 产品指标

在用户明确同意且坚持最小化采集的前提下，可关注：

- 首次创建连接成功率。
- 首次连接成功所需时间。
- 连接失败分类占比，但不采集服务器地址。
- SSH 会话异常断开率。
- 上传/下载任务成功率和取消率。
- 无崩溃会话比例、卡顿率和内存异常增长。
- 连接诊断解决问题后的重试成功率。

不得采集终端输入输出、远程路径、文件名、文件内容、密码、私钥或完整服务器标识。

## 16. 待产品决策

以下问题不阻塞 0.5.0，但应在 1.0 前确定：

1. 采用官网分发、开源发布还是 Mac App Store；端口转发和系统 SSH 对 Sandbox 策略有影响。
2. 产品是免费、买断、订阅还是开源增值模式。
3. 是否支持 iCloud 同步，以及凭据是否永远保持本机。
4. 是否提供遥测；若提供，默认关闭还是首次启动明确询问。
5. 正式产品是否只支持 Apple Silicon，是否永不兼容 Intel Mac。
6. 团队功能是否作为独立版本，而不是持续扩大个人版复杂度。
7. AI 辅助是否立项；在隐私边界未确定前不加入核心流程。

## 17. 当前建议

下一阶段不建议立即增加大量高级功能。最合理的顺序是：

1. 以当前 0.5.3-alpha 为稳定基线，继续回归密码、密钥、内网、公网、主机密钥、文件和监控主流程。
2. 下一版本优先补齐下载进度/断点续传、批量文件操作、分屏与多窗口，不再扩大不稳定入口。
3. 对带凭据上游代理使用独立安全隧道实现，禁止把代理密码写入命令行或普通配置。
4. 确定发布渠道后集中处理 Developer ID、公证、Hardened Runtime、自动更新和迁移回滚，形成公开分发版本。

这样可以优先解决“能不能稳定、顺畅地完成任务”，再扩展“能完成多少种高级任务”。
